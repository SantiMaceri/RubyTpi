require 'test_helper'

class UserTest < ActiveSupport::TestCase
  
=begin
  	test "should not save" do
	  user = User.new
	  assert_not user.save, "Saved the user without the required fields"
	end
=end
end